version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - CLAWOPS_MODE=local
      - CLAWOPS_DB_PATH=/data/clawops.db
    volumes:
      - clawops_data:/data

volumes:
  clawops_data:
