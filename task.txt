You are a senior engineer on ClawOps — an open source operations layer for AI agent teams.

ISSUE #20: packages/analytics — token and cost tracking
Branch: feat/gemini/issue-20
Repo: https://github.com/hishamank/clawops

## Read first (mandatory — use your full context window)
- claude.md — full project context and data model
- agents.md — how to work on this codebase
- packages/core/src/schema.ts — usageLogs table schema
- packages/domain/src/modelPricing.ts — pricing table and getModelPricing()
- packages/domain/src/utils.ts — calcCost()
- packages/agents/src/index.ts — understand the agents structure
- packages/tasks/src/index.ts — understand tasks structure

## Your job
Implement analytics business logic in packages/analytics/src/index.ts

## Deliverables
- [ ] packages/analytics/package.json — add @clawops/core and @clawops/domain as dependencies
- [ ] packages/analytics/tsconfig.json — extends ../../tsconfig.base.json
- [ ] packages/analytics/src/index.ts:

  - logUsage(db, input: { agentId: string, taskId?: string, model: string, tokensIn: number, tokensOut: number }): UsageLog
    - Calculate cost using calcCost() from @clawops/domain
    - Insert into usageLogs table
    - Return the created row

  - getTokenStats(db, filters?: { agentId?: string, model?: string, from?: Date, to?: Date }): { totalTokensIn: number, totalTokensOut: number, totalCost: number, count: number }

  - getCostByAgent(db, from?: Date, to?: Date): Array<{ agentId: string, totalCost: number, totalTokens: number }>
    - Group by agentId, sorted by totalCost descending

  - getCostByModel(db, from?: Date, to?: Date): Array<{ model: string, totalCost: number, totalTokens: number }>
    - Group by model, sorted by totalCost descending

  - getCostByProject(db, from?: Date, to?: Date): Array<{ projectId: string | null, totalCost: number, totalTokens: number }>
    - Join usageLogs → tasks → projectId, group by projectId

  - getDailySpend(db, from: Date, to: Date): Array<{ date: string, cost: number, tokensIn: number, tokensOut: number }>
    - date format: YYYY-MM-DD
    - One row per day in the range, sorted ascending
    - Days with no usage still appear with cost: 0

- [ ] pnpm typecheck passes with zero errors

## Rules
- TypeScript strict mode, no any
- Synchronous — better-sqlite3 is sync, use .all()/.get()
- Drizzle ORM for simple queries; raw SQL allowed ONLY in this package for aggregation queries where Drizzle groupBy/sum is insufficient
- Explicit return types on all exported functions

## When done
1. git add -A && git commit -m "feat: implement packages/analytics — token and cost tracking (#20)"
2. git push -u origin feat/gemini/issue-20
3. Run: openclaw system event --text "Done: Issue 20 packages/analytics complete, PR ready" --mode now
