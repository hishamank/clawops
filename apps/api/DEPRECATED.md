# apps/api (Deprecated)

`apps/api` is deprecated and retained temporarily for rollback safety.

Current transport is implemented in Next.js route handlers under `apps/web/app/api/*`.

Do not add new features to `apps/api`.
