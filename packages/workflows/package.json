{
  "name": "@clawops/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "eslint src/",
    "test": "pnpm build && node --experimental-test-module-mocks --import tsx/esm --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "better-sqlite3": "^11.7.0",
    "drizzle-orm": "^0.38.0",
    "typescript": "^5.7.0",
    "tsx": "^4.21.0"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "dependencies": {
    "@clawops/core": "workspace:*",
    "@clawops/tasks": "workspace:*",
    "@clawops/notifications": "workspace:*",
    "@clawops/sync": "workspace:*"
  }
}
