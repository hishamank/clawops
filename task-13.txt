You are a senior engineer working on ClawOps — an open source operations layer for AI agent teams.

ISSUE #13: packages/shared — types, constants, model pricing
Branch: feat/claude/issue-13-shared
Repo: https://github.com/hishamank/clawops

## Context
Read these files first:
- claude.md (project context, full data model, coding principles)
- agents.md (how to work on this codebase)
- packages/shared/package.json (if exists)
- packages/core/src/schema.ts

## Your job
Build the packages/shared package from scratch. This package has NO dependencies on other @clawops/* packages. Everything else imports from here.

## Deliverables
- [ ] packages/shared/package.json — name: @clawops/shared, exports ./src/index.ts, devDeps: typescript
- [ ] packages/shared/tsconfig.json — extends ../../tsconfig.base.json
- [ ] packages/shared/src/modelPricing.ts — pricing table:
  - claude-opus-4-6: { inputPer1M: 15.00, outputPer1M: 75.00 }
  - claude-sonnet-4-6: { inputPer1M: 3.00, outputPer1M: 15.00 }
  - claude-haiku-4-5: { inputPer1M: 0.25, outputPer1M: 1.25 }
  - gpt-4o: { inputPer1M: 5.00, outputPer1M: 15.00 }
  - gpt-4o-mini: { inputPer1M: 0.15, outputPer1M: 0.60 }
  - llama3: { inputPer1M: 0, outputPer1M: 0 }
  - qwen2.5: { inputPer1M: 0, outputPer1M: 0 }
  - gemini-2.0-flash: { inputPer1M: 0.10, outputPer1M: 0.40 }
  - "ollama/*": wildcard default { inputPer1M: 0, outputPer1M: 0 }
  - Export: getModelPricing(model: string) function that handles ollama/* wildcard
- [ ] packages/shared/src/constants.ts — typed const objects for:
  - AgentStatus: online | idle | busy | offline
  - TaskStatus: backlog | todo | in-progress | review | done | cancelled
  - TaskPriority: low | medium | high | urgent
  - ProjectStatus: planning | active | paused | done
  - MilestoneStatus: pending | done
  - HabitType: heartbeat | scheduled | cron | hook | watchdog | polling
  - HabitStatus: active | paused
  - IdeaStatus: raw | reviewed | promoted | archived
  - Source: human | agent | cli | script
- [ ] packages/shared/src/utils.ts:
  - generateId() — returns a nanoid-style 21-char random string (use crypto.randomUUID or nanoid)
  - hashApiKey(key: string): string — SHA-256 hex hash using node:crypto
  - verifyApiKey(key: string, hash: string): boolean
  - formatCost(cost: number): string — formats as "$0.0042" with 4 decimal places
  - calcCost(model: string, tokensIn: number, tokensOut: number): number
- [ ] packages/shared/src/types.ts — shared TypeScript interfaces not inferred from Drizzle
- [ ] packages/shared/src/index.ts — re-exports everything from all 4 files
- [ ] pnpm typecheck passes for this package

## Rules
- TypeScript strict mode, no any
- Use node:crypto for hashing — no external crypto libs
- Keep it minimal — only what is genuinely shared

## When completely done
1. git add -A && git commit -m "chore: build packages/shared with types, constants, pricing, utils (#13)"
2. git push -u origin feat/claude/issue-13-shared
3. Run: openclaw system event --text "Done: Issue 13 packages/shared complete, PR ready" --mode now
